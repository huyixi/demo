import Link from "next/link";

import { siteConfig } from "@/app/config/site";

export default function SiteHeader() {
  return <header className="sticky top-0 z-50 w-full border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60"></header>;
}
